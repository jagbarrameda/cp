// could not solve it
// learning: look for and find the trick.
// The trick involved looking at ways to win for alice.
// I tried to find all the combinations, screwed.
fun main() {
    var t = readln().toInt()
    while (t-- > 0) {
        var (n, nqs) = readln().split(' ').map { it.toInt() }
        val a = readln().split(' ').map { it.toInt() }
        val s = readln()
        while (nqs-->0) {
            val q = readln().toInt()

        }
    }
}