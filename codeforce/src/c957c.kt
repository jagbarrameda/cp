import java.util.Arrays.sort
import java.util.PriorityQueue

fun main() {
    var t = readln().toInt()
    while (t-- > 0) {
        var (n, m, q) = readln().split(' ').map { it.toInt() }
        // this is the easy version which means q = 0
        var a = readln().split(' ').map { it.toInt() }
        var b = readln().split(' ').map { it.toInt() }
        var ans = "YA"
        println(ans)
    }
}
